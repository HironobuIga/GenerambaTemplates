//
//  {{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

import UIKit

import RxSwift
import RxCocoa

final class {{ module_info.name }}ViewController: UIViewController {

    // MARK: - IBOutlet

    // MARK: - Property
    private var viewModel: {{ module_info.name }}ViewModelType!
    private let disposeBag = DisposeBag()

    // MARK: - Life cycle
    static func insntantiate(viewModel: {{ module_info.name }}ViewModelType? = nil) -> {{ module_info.name }}ViewController {
        let viewController = R.storyboard.{{ module_info.name }}ViewController.instantiateInitialViewController()!
        let wireframe = Default{{ module_info.name }}Wireframe(on: viewController)
        let viewModel = viewModel ?? {{ module_info.name }}ViewModel(dependency: wireframe)
        viewController.viewModel = viewModel
        return viewController
    }

    override func viewDidLoad() {
        super.viewDidLoad()
        setupView()
        bindTo()
    }

    // MARK: - Function

}

// MARK: - Private Function
private extension {{ module_info.name }}ViewController {

    func setupView() {

    }

    func bindTo() {

    }
}
